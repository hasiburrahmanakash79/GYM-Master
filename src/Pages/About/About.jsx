import SectionTitle from "../../Hooks/SectionTitle";

const About = () => {
    return (
        <div>
            <SectionTitle subTitle={"Hook"}></SectionTitle>
            <h1>About</h1>
        </div>
    );
};

export default About;